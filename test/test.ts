import assert from 'assert';

describe('My function', function() {
    it('should test', function() {
        // var Siphiwe = 1;
        // var Malinga = 2;
        assert.equal(1, 1);
    });
});